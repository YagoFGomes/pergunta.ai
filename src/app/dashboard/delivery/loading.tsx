import ModuleLoading from '@/features/platform/components/module-loading';

export default function Loading() {
  return (
    <ModuleLoading
      pageTitle='Logs de Entrega'
      pageDescription='Carregando logs de envio e falhas.'
    />
  );
}
