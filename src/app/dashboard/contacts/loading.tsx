import ModuleLoading from '@/features/platform/components/module-loading';

export default function Loading() {
  return <ModuleLoading pageTitle='Contatos' pageDescription='Carregando listas e contatos.' />;
}
